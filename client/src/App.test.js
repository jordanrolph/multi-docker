import React from "react";
import { render } from "@testing-library/react";
import App from "./App";

test("renders", () => {
  // removed because Fib makes network req on mount
  // and we haven't mocked this in Travis
});
